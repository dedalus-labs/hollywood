# @generated by Hollywood. Do not edit by hand.

name: Runner image
on:
  pull_request:
    branches:
      - main
    paths:
      - runner/**
      - src/container*.ts
      - src/github-runner*.ts
      - src/runner*.ts
      - gha/actions.ts
      - gha/runner-image*.ts
      - .npmrc
      - package.json
      - package-lock.json
      - tsdown.config.ts
  push:
    branches:
      - main
    paths:
      - runner/**
      - src/container*.ts
      - src/github-runner*.ts
      - src/runner*.ts
      - gha/actions.ts
      - gha/runner-image*.ts
      - .npmrc
      - package.json
      - package-lock.json
      - tsdown.config.ts
  release:
    types:
      - published
  schedule:
    - cron: 17 7 * * 1
  workflow_dispatch: {}
concurrency:
  group: runner-image-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  observe:
    name: Observe (${{ matrix.architecture }})
    if: ${{ github.repository == 'dedalus-labs/hollywood' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && (github.event_name != 'release' || github.event_name == 'release' && startsWith(github.ref, 'refs/tags/runner-v')) }}
    runs-on: ${{ matrix.architecture == 'amd64' && 'ubuntu-24.04' || 'ubuntu-24.04-arm' }}
    strategy:
      matrix:
        architecture:
          - amd64
          - arm64
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          persist-credentials: false
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: "24"
      - name: Install dependencies
        run: npm ci
      - name: Audit dependencies
        run: npm audit --audit-level=high
      - name: Verify registry signatures
        run: npm audit signatures
      - name: Build Hollywood
        run: npm run build
      - name: Build local actions
        run: npm run actions
      - name: Capture runner
        uses: ./.github/actions/capture-runner-probe
        with:
          output: runner-${{ matrix.architecture }}.json
      - name: Verify runner
        uses: ./.github/actions/verify-runner-probe
        with:
          contract: runner/contract.json
          probe: runner-${{ matrix.architecture }}.json
      - name: Upload runner probe
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: runner-${{ matrix.architecture }}
          path: runner-${{ matrix.architecture }}.json
          if-no-files-found: error
          retention-days: 30
  verify:
    name: Verify image (${{ matrix.provider }}, ${{ matrix.architecture }})
    if: ${{ github.repository == 'dedalus-labs/hollywood' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && (github.event_name != 'release' || github.event_name == 'release' && startsWith(github.ref, 'refs/tags/runner-v')) }}
    runs-on: ${{ matrix.architecture == 'amd64' && 'ubuntu-24.04' || 'ubuntu-24.04-arm' }}
    strategy:
      fail-fast: false
      matrix:
        architecture:
          - amd64
          - arm64
        provider:
          - docker
          - podman
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          persist-credentials: false
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: "24"
      - name: Install dependencies
        run: npm ci
      - name: Audit dependencies
        run: npm audit --audit-level=high
      - name: Verify registry signatures
        run: npm audit signatures
      - name: Build Hollywood
        run: npm run build
      - name: Build local actions
        run: npm run actions
      - name: Verify runner image
        uses: ./.github/actions/verify-runner-image
        with:
          provider: ${{ matrix.provider }}
  publish:
    name: Publish image
    needs:
      - observe
      - verify
    if: ${{ github.repository == 'dedalus-labs/hollywood' && github.event_name == 'release' && startsWith(github.ref, 'refs/tags/runner-v') }}
    runs-on: ubuntu-24.04
    permissions:
      attestations: write
      contents: read
      id-token: write
      packages: write
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          persist-credentials: false
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: "24"
      - name: Install dependencies
        run: npm ci
      - name: Audit dependencies
        run: npm audit --audit-level=high
      - name: Verify registry signatures
        run: npm audit signatures
      - name: Build Hollywood
        run: npm run build
      - name: Build local actions
        run: npm run actions
      - id: release
        name: Prepare release
        uses: ./.github/actions/prepare-runner-image-release
        with:
          image: ghcr.io/dedalus-labs/hollywood-runner
          ref: ${{ github.ref }}
          ref-name: ${{ github.ref_name }}
          revision: ${{ github.sha }}
      - name: Log in to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - name: Set up QEMU
        uses: docker/setup-qemu-action@c7c53464625b32c7a7e944ae62b3e17d2b600130
      - name: Set up Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f
      - id: build
        name: Build and publish
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8
        with:
          context: runner
          file: runner/Containerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.release.outputs.tags }}
          labels: |-
            ${{ format('org.opencontainers.image.base.name={0}
            org.opencontainers.image.version={1}
            io.dedalus.hollywood.github-actions-runner.version={2}', 'ghcr.io/actions/actions-runner@sha256:0cfdcc701ce933c6d243c6b0b2da767366dc9f2e99961d4c3754b0b78084cdda', steps.release.outputs.version, '2.336.0') }}
          build-args: ${{ format('SOURCE_REVISION={0}', github.sha) }}
          cache-from: type=gha,scope=runner-image-main
          cache-to: type=gha,mode=max,scope=runner-image-main
          provenance: mode=max
          sbom: true
      - name: Attest image
        uses: actions/attest-build-provenance@43d14bc2b83dec42d39ecae14e916627a18bb661
        with:
          subject-name: ghcr.io/dedalus-labs/hollywood-runner
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true
      - env:
          GH_TOKEN: ${{ github.token }}
        name: Verify publication
        uses: ./.github/actions/verify-published-runner-image
        with:
          digest: ${{ steps.build.outputs.digest }}
          image: ghcr.io/dedalus-labs/hollywood-runner
          repository: dedalus-labs/hollywood
          source-digest: ${{ github.sha }}
          source-ref: ${{ steps.release.outputs.source-ref }}
