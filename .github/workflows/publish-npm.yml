# @generated by Hollywood. Do not edit by hand.

name: Publish release
on:
  push:
    branches:
      - main
    paths:
      - .release-please-manifest.json
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  detect:
    name: Detect release components
    runs-on: ubuntu-latest
    outputs:
      hollywood: ${{ steps.components.outputs.hollywood }}
      hollywood-tag: ${{ steps.components.outputs.hollywood-tag }}
      runner: ${{ steps.components.outputs.runner }}
      runner-tag: ${{ steps.components.outputs.runner-tag }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: "24"
      - name: Install dependencies
        run: npm ci
        shell: bash
      - name: Build Hollywood
        run: npm run build
        shell: bash
      - name: Build local actions
        run: npm run actions
        shell: bash
      - id: components
        name: Detect components
        uses: ./.github/actions/detect-release-components
        with:
          before: ${{ github.event.before }}
          current: ${{ github.sha }}
  publish:
    name: Publish
    needs: detect
    if: ${{ github.repository == 'dedalus-labs/hollywood' && needs.detect.outputs.hollywood == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment:
      name: npm
      url: https://www.npmjs.com/package/@dedalus-labs/hollywood
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: "24"
          registry-url: https://registry.npmjs.org
      - name: Install dependencies
        run: npm ci
        shell: bash
      - name: Lint
        run: npm run lint
        shell: bash
      - name: Typecheck
        run: npm run typecheck
        shell: bash
      - name: Test
        run: npm test
        shell: bash
      - name: Build
        run: npm run build
        shell: bash
      - name: Build local actions
        run: npm run actions
        shell: bash
      - name: Check Hollywood state
        run: node dist/cli.js check
        shell: bash
      - name: Publish to npm
        uses: ./.github/actions/publish-npm
  release:
    name: Create GitHub Release
    needs:
      - detect
      - publish
    if: ${{ always() && github.repository == 'dedalus-labs/hollywood' && needs.detect.result == 'success' && (needs.publish.result == 'success' || needs.publish.result == 'skipped') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          persist-credentials: false
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: "24"
      - name: Install dependencies
        run: npm ci
        shell: bash
      - name: Build Hollywood
        run: npm run build
        shell: bash
      - name: Build local actions
        run: npm run actions
        shell: bash
      - id: cind-token
        name: Create Cind app token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1
        with:
          client-id: ${{ secrets.CIND_BOT_CLIENT_ID }}
          private-key: ${{ secrets.CIND_BOT_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: hollywood
          permission-contents: write
          permission-issues: write
          permission-metadata: read
          permission-pull-requests: write
      - id: release
        name: Create draft releases
        uses: googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7
        with:
          token: ${{ steps.cind-token.outputs.token }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
          skip-github-pull-request: "true"
      - name: Publish immutable releases
        uses: ./.github/actions/publish-draft-releases
        with:
          hollywood-tag: ${{ needs.detect.outputs.hollywood-tag }}
          repository: ${{ github.repository }}
          runner-tag: ${{ needs.detect.outputs.runner-tag }}
          token: ${{ steps.cind-token.outputs.token }}
