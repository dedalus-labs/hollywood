# @generated by Hollywood. Do not edit by hand.

name: Contributor Checks
on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review
  merge_group:
    types:
      - checks_requested
permissions:
  contents: read
jobs:
  cla:
    name: CLA
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout contributor check implementation
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: "24"
      - name: Install dependencies
        run: npm ci
        shell: bash
      - name: Build Hollywood
        run: npm run build
        shell: bash
      - name: Build local actions
        run: npm run actions
        shell: bash
      - name: Check CLA
        uses: ./.github/actions/check-cla
        with:
          author: ${{ github.event.pull_request.user.login }}
          bootstrap-maintainers: windsornguyen
          trusted-automation-authors: cind-bot[bot] cind[bot]
  vouch:
    name: Vouch
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    needs: cla
    steps:
      - name: Checkout contributor check implementation
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: "24"
      - name: Install dependencies
        run: npm ci
        shell: bash
      - name: Build Hollywood
        run: npm run build
        shell: bash
      - name: Build local actions
        run: npm run actions
        shell: bash
      - name: Check Vouch
        uses: ./.github/actions/check-vouch
        with:
          author: ${{ github.event.pull_request.user.login }}
          bootstrap-maintainers: windsornguyen
          trusted-automation-authors: cind-bot[bot] cind[bot]
