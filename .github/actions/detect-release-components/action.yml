# @generated by Hollywood. Do not edit by hand.

name: Detect release components
description: Identify the Release Please component versions changed by the current commit.
inputs:
  before:
    description: Git revision before the release commit.
    required: false
    default: ""
  current:
    description: Current release commit revision.
    required: true
outputs:
  hollywood:
    description: Whether the npm package version changed.
  runner:
    description: Whether the runner image version changed.
runs:
  using: node24
  main: dist/index.js
