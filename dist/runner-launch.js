/* @generated by npm run build. Do not edit by hand. */
import { spawn } from "node:child_process";
//#region src/runner-launch.ts
const jitConfig = process.env["HOLLYWOOD_RUNNER_JIT_CONFIG"];
if (jitConfig === void 0 || jitConfig === "") throw new Error("HOLLYWOOD_RUNNER_JIT_CONFIG is required.");
delete process.env["HOLLYWOOD_RUNNER_JIT_CONFIG"];
const runner = spawn("/home/runner/bin/Runner.Listener", [
	"run",
	"--jitconfig",
	jitConfig
], {
	env: process.env,
	stdio: "inherit"
});
for (const signal of [
	"SIGINT",
	"SIGTERM",
	"SIGHUP"
]) process.once(signal, () => runner.kill(signal));
const exitCode = await new Promise((resolve, reject) => {
	runner.once("error", reject);
	runner.once("close", (code, signal) => {
		if (signal !== null) {
			reject(/* @__PURE__ */ new Error(`Runner.Listener exited after signal ${signal}.`));
			return;
		}
		resolve(code ?? 1);
	});
});
if (exitCode !== 0) throw new Error(`Runner.Listener exited with code ${exitCode}.`);
//#endregion
export {};
